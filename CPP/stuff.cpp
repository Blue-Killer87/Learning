#include <iostream>
#include <cstring>

#define const 123

int main(){
    int helper;
    char text;
    std::cin >> text;

    for (i=0, i<strlen(text), i++){
        std::cout << text[i];
    }

    return 0;
}